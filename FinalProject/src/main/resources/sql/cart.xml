<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Carts">
	
	<insert id="insert" parameterType="map">
		insert into cart(cart_num, product_code, id, order_de_count)
		values(cart_seq.nextval, #{product_code}, #{id}, #{order_de_count})
	</insert>
	
	<select id="list" resultType="cart" parameterType="map">
		select *
		from (select rownum rnum, c.*
			  from (select *
			  		from cart
			  		order by add_date desc) c
			 )
		where rnum &gt;= #{start} and rnum &lt;= #{end}
	</select>
	
	<select id="count" resultType="int" parameterType="int">
		select count(*)
		from cart
		where id = ${id}
	</select>
	
</mapper>
